/**
 * FILE PATH: /ejdk/ejidike-foundation/app/admin/content/layout.tsx
 * PURPOSE: Layout for admin content management pages
 */

import { ReactNode } from 'react';

export default function AdminContentLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}